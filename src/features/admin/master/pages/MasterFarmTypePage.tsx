'use client';

import { MasterDataPage } from './MasterDataPage';

export function MasterFarmTypePage() {
  return (
    <MasterDataPage
      title="ประเภทการเลี้ยง"
      subtitle="จัดการข้อมูลประเภทการเลี้ยงในระบบ (เช่น หมูขุน, หมูอนุบาล)"
    />
  );
}
